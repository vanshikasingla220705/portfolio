"use client";
import { motion } from "framer-motion";

export default function Header() {
  return (
    <header>
      <motion.div className=' top-0 left-0 h-800 w-full bg-red-500'>
        header working
      </motion.div>
    </header>
  );
}
